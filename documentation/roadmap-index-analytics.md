# Roadmap — MERGED into the single roadmap

> **This doc was merged into the one roadmap on 2026-06-13** (owner: "une seule roadmap; fusionner
> les docs similaires"). The destination **and** the build order now live in one place:
> **[`../TARGET.md`](../TARGET.md)**.

`TARGET.md` is the single strategic roadmap — the money thesis (§1), the strategy book S1–S5
(§3, dispersion is the flagship), the standing rulings R1–R4 (§4), the capability map with live
state (§5), the robustness standard (§6), and the **pre-ordered gap list (§7) that sequences what
we build next**. It is the strategy agents follow to orient; `../tasks/` holds the tactics.

Where the content of this file went:
- **Phase 0→3 sequence / "plan of record"** → TARGET §7 (gap list, pre-ordered) + the phase
  groupings on `../tasks/TASKBOARD.md`.
- **OQ-1…4 rulings** (monetization, history source, point-in-time membership, tenor grid) and the
  forward-vs-futures ruling → `../.agent/open-questions.md` (the canonical decision register).
- **Tab 1 / Tab 2 / Tab 3 front intent** → TARGET §2 (week goal) + §5; the per-page detail and the
  page-1 cahier remain in `cahier_des_charges_dashboard_page1.md`.
- **Scope (index-only, SX5E-first, SPX parked, Saxo/Deribit removed)** → TARGET §0 + ADR 0042.

The course brief that seeded all of this is in [`transcripts/`](transcripts/); the blueprint
(`blueprint/`, ADR 0011) is the domain authority on any formula/field/tenor conflict.
