# T-index-only-refactor — collapse the app to index-options-only (SX5E first, SPX parked, Saxo/Deribit removed)

> **Owner ruling 2026-06-13** (memory `index-only-app-scope`). The app is FOR index options
> trading (component logic, baskets). To stabilise, focus on a functional app on **EuroStoxx
> 50 (SX5E)** first. Keep **SPX parked** (`enabled:false`, not deleted — cheapest multi-index
> proof). **Remove Saxo + Deribit** entirely (leaf adapters, no core coupling; git history is
> the archive). Single-names are **index constituents, never option underlyings** — purge them
> as standalone underlyings, keep `data/reference/index_constituents/`.
>
> Folds in the parameterisation audit (`AUDIT-INTENT-VS-DELIVERY-2026-06-12`): "delete
> everything that is not EuroStoxx or S&P". Reverses C1's "keep Saxo/Deribit" (ADR 0023).

## Constraint — shared working tree
A concurrent agent owns the delta-band fix and touches `surfaces/projection.py`,
`actor/driver.py`, `configs/qc.yaml`, `test_analytics_projection.py`,
`test_surfaces_projection.py`. **Do those-overlapping edits LAST**, after the delta fix lands,
to avoid clobbering. Stage by explicit path, never `git add -A`.

## Phases (recommended order)

**Phase 2 — park SPX** (trivial, no code)
- `configs/universe.yaml`: `SPX.enabled: true → false`; fix the stale "both seed entries
  enabled" comment. Regenerate universe config-hash golden by design (ADR 0028).

**Phase 1 — remove Saxo/Deribit** (bulk)
- Delete `packages/infra-saxo/` (59 f.) + `packages/infra-deribit/` (32 f.) + tests.
- `pyproject.toml`: workspace members (l.16-17), `mypy_path` (l.79), import-linter layer
  (l.168 → IBKR only), `testpaths` (l.196-197).
- Frontend: `app.py:112` (`saxo_oauth`), `routers/oauth.py` (`SaxoOAuthSettings` + 4 `/saxo/*`
  endpoints), `providers.py` (SAXO+DERIBIT capability blocks), `test_oauth_api.py` (8 tests).
- Configs: `broker.yaml` comment, `universe.yaml:60` comment, delete `forwards_saxo/deribit.yaml`.
- Infra tests with `provider="SAXO"/"DERIBIT"` rows → rebase on IBKR (test_storage_provider,
  test_collection_use_cases sessions assert, test_p0_contracts, **test_analytics_projection
  — DEFER, delta-agent file**).
- ADRs 0013 (deribit) / 0014 (saxo) → mark superseded/parked.

**Phase 3 — purge single-names as option underlyings** ✅ DONE (commit `c5b456c`)
- Removed `UniverseConfig.underlyings` field entirely (one universe source = the index
  registry; owner coherence principle). `platform_config.py` field, `jobs.py` log bind,
  `configs/universe.yaml`, config tests + ~12 infra test call sites updated. universe
  config-hash golden regenerated by design (d41c8d2d→881ef3c6; qc/pricing/scenarios
  byte-identical). `data/reference/index_constituents/` KEPT intact.

**Phase 4 — audit hook + gate**
- Fold `T-capture-config-coherence` (orphan `capture.yaml` leaves with the index scope).
- `just gate` green (ruff+mypy+lint-imports+pytest) + web gate; update TASKBOARD.

## Phase 1 — Saxo/Deribit removal log (DONE 2026-06-13, gate green)

**Why.** The app is index-options-only with IBKR as the sole live broker (memory
`index-only-app-scope`). Saxo and Deribit were dead weight: leaf adapters with **zero core
import coupling** (verified — nothing outside their own dirs imports `infra_saxo`/`infra_deribit`).
They are not being used and add real surface (91 files, OAuth, broker seams, tests). Git history
is the archive — recover with `git checkout <pre-removal-sha> -- packages/infra-saxo` if ever
needed. Reverses the "keep Saxo/Deribit" line of ADR 0023; ADRs 0013/0014 flipped to *superseded*.

**Removed (whole dirs).**
- `packages/infra-saxo/` (59 files) + `packages/infra-deribit/` (32 files) + their tests/configs/samples.
- `apps/frontend/.../routers/oauth.py` — the entire `/api/oauth` router was the **Saxo** authorization-code flow; dead with Saxo gone.
- `apps/frontend/.../oauth_state.py` — the OAuth CSRF store; used **only** by the Saxo router.
- `apps/frontend/tests/test_oauth_api.py` — 8 tests, all Saxo OAuth.

**Unwired (edited, not deleted), with why:**
| File | Edit | Why |
|---|---|---|
| `pyproject.toml` | drop `infra-saxo`/`infra-deribit` from workspace members, `mypy_path`, import-linter `layers` (broker layer → `infra_ibkr` only), pytest `testpaths` | the packages no longer exist; the layering contract must name the surviving leaf only |
| `apps/frontend/.../app.py` | remove `OAuthStateStore` import + `app.state.oauth_states` + `app.state.saxo_oauth` + the `oauth` router import/mount; docstring trimmed | the OAuth router and its state are gone |
| `apps/frontend/.../providers.py` | delete the `SAXO` + `DERIBIT` `ProviderCapability` rows; docstring trimmed | the run-endpoint provider selector must not advertise removed brokers |
| `apps/frontend/.../__init__.py` | drop the `/api/oauth` line from the router list | that route no longer exists |
| `apps/frontend/tests/test_run_api.py` | provider-list assertion now asserts `SAXO`/`DERIBIT` **absent** (was: `SAXO.auth_required is True`) | locks the removal so a re-add is caught |
| `configs/broker.yaml`, `configs/universe.yaml` | comments: drop `infra-saxo`/`saxo:`/`deribit:` mentions; note IBKR is the sole live broker, provider dimension (ADR 0017) stays generic | no stale pointers to deleted packages |
| `.agent/decisions/0013`, `0014` | status → *superseded* with a recovery note | traceability of the reversal |

**Deliberately KEPT (not a bug):** provider-string fixtures in `packages/infra/tests`
(`provider="SAXO"/"DERIBIT"`) and the generic provider-dimension code in `packages/infra/src`
(`storage/events.py`, `contracts/tables.py`, `collectors/*`, `index_registry.py`…). These test
and implement the **partition-by-provider** capability (ADR 0017), which is generic infra — not a
dependency on the deleted packages. They pass unchanged. (Cosmetic docstring mentions of the old
package paths in `infra/src` remain; low-value, can be swept later.)

**Verification:** `ruff` ✓ · `mypy` ✓ (209 files) · `lint-imports` ✓ (2 contracts kept) ·
`pytest` ✓ on `apps/frontend/tests` + provider-string infra tests. Only the pre-existing
`documentation/`-deletion failures remain (owner: leave).

## Done criteria
Saxo/Deribit gone (build/gate/mypy/lint-imports clean without them); SPX parked via flag;
no single-name option underlyings; `index_constituents` intact; goldens regenerated by design;
`just gate` + web green; TASKBOARD + audit report updated.
